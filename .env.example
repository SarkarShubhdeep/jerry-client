# Optional dev overrides (Electron main process only)
# LLM API keys are configured in-app via Settings, not here.

# ActivityWatch REST API (optional; default http://localhost:5600/api/0)
# ACTIVITYWATCH_BASE_URL=http://localhost:5600/api/0
